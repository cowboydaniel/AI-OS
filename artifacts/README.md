# Artifacts Directory

This directory collects generated outputs such as installer ISOs and their checksums. Keep binaries out of Git history—ISOs under `artifacts/iso/` are tracked via Git LFS and checksum files belong in `artifacts/checksums/`. Do not commit any other generated logs or caches.
