# Checksums

Place checksum files corresponding to ISOs in `../iso/` here. Use deterministic filenames (e.g., `aetheros-0.1.0.sha256`) and include a single line per file generated with `sha256sum`. Signatures can live alongside checksums when available.
