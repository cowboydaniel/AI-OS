# Scripts Directory

Place developer utilities here, including bootstrap helpers, linting entrypoints, ISO build orchestration, and release automation. Scripts should be POSIX shell or Python where possible, include `set -euo pipefail` for safety, and document prerequisites near the top of each file.
